[package]
name = "umni"
version = "0.2.0"
edition = "2024"
license = "GPL"

[dependencies]
rand = "0.10"
serde_json = "1.0"
clap = { version = "4.6", features = ["derive"] }
serde = { version = "1.0", features = ["derive"] }

[profile.release]
codegen-units = 1
